INSTALL VERSION 3.10 PYTHON


pip install -r requirements.txt


in MySQL run command -create database lateattendance

Run in Python terminal

flask db init

flask db migrate -m "message"

flask db upgrade
